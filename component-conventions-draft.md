# 组件通用规范（草案 / 骨架）

- 状态：**骨架草案，未回写飞书**（按铁律 12，设计阶段只产本地文档）
- 拟承载位置：新建飞书 wiki 文档《组件通用规范》，在《组件库范围与 MVP 清单》§3 留引用、§13.3 登记链接
- 上游真值源：《CSS Token 映射表 V1》（`N8CUw90lViYpmPkbuVrc8675nsf`）——**Token 值以映射表为准，本规范只管「怎么用」**
- 横向基准：《Button 组件 API 契约》（`Pc6AdxcjJoNmztxYPcNcMUSeniB`，rev 1497）
- 起草日期：2026-09-24

---

## 设计说明：为什么需要这份规范

本库已有 Button（契约 rev 1497 + 实现）与 Input（契约 rev 226 + 实现）在推进。**经逐项取证（2026-09-24），偏差的真实位置是「契约 ↔ 实现」，而非「组件 ↔ 组件」**——两份契约在尺寸体系上**已经对齐**：

| 项             | Button 契约 (rev 1497)                       | Button 实现 (`button.tsx`)                                    | Input 契约 (rev 226)     | Input 实现 (`input.tsx`) | 判定                                           |
| -------------- | -------------------------------------------- | ------------------------------------------------------------- | ------------------------ | ------------------------ | ---------------------------------------------- |
| `size` 枚举    | `small`/`medium`/`large`                     | `default`/`xs`/`sm`/`lg`/`icon`/`icon-xs`/`icon-sm`/`icon-lg` | `small`/`medium`/`large` | 无（未用 cva）           | 契约已一致；**实现落后**（仍是 shadcn 官方集） |
| 默认值         | `medium`                                     | `default`                                                     | `medium`                 | —                        | 契约已一致                                     |
| 高度           | 24/32/40                                     | 28/32/36/40（icon 系列 28/32/36/40）                          | 24/32/40                 | 硬编码 `h-9`(36px)       | **契约已一致**；实现落后                       |
| 圆角           | **`rounded-sm` 4px**                         | `rounded-md` 6px                                              | `rounded-md` 6px         | `rounded-md` 6px         | **以 4px 为准（A2 已裁决）→ 其余三处需改**     |
| 水平内边距     | 8（带图标 6）/16（12）/24（16）              | 10/12/16/24                                                   | 8/16/24                  | `px-3`(12px)             | 契约相近；**「带图标收窄」规则仅 Button 有**   |
| 字号           | 12/14/14                                     | 12(`xs`)/14(基础)                                             | 12/14/14                 | `text-base md:text-sm`   | 契约已一致；实现为规避 iOS 聚焦缩放            |
| 图标尺寸       | 14/16/16                                     | `[&_svg]:size-4`(16px) 统一                                   | 14/16/16                 | —                        | 契约已一致                                     |
| icon-only 尺寸 | 枚举未定义，但 §组合规则引用了 `size="icon"` | 4 档（`icon`/`icon-xs`/`icon-sm`/`icon-lg`）                  | 无此概念                 | —                        | **Button 契约内部不一致**（引用未定义枚举）    |

**真正需要规范裁决的三项**：

| #   | 议题                                             | 现状                                                                          |
| --- | ------------------------------------------------ | ----------------------------------------------------------------------------- |
| A   | **控件圆角**：`radius/sm` 4px vs `radius/md` 6px | Button 契约 4px（唯一）；Button 实现 / Input 契约 / Input 实现均 6px          |
| B   | **「带图标时内边距收窄」是否为全库规则**         | Button 契约有（8→6 / 16→12 / 24→16）；Input 契约无                            |
| C   | **icon-only 尺寸是否纳入规范**                   | Button 需要（实现有 4 档、契约引用了 `size="icon"` 但未定义）；Input 无此概念 |

**已一致、只需固化为 MUST 的部分**：`size` 枚举命名 `small`/`medium`/`large`、默认 `medium`、高度阶梯 **24/32/40px**、字号 **12/14/14**、图标尺寸 **14/16/16**。

> 另需规范收口的全库级偏差（不属尺寸体系，见 §4 / §5）：聚焦环与错误环口径（双双偏离映射表 `focus:ring-2`；50% 实测 1.71:1、`/20` 实测 1.34:1 均不达 1.4.11）、disabled 两套口径（Button `opacity-50` vs Input 换底色）、hover 一有一无。
>
> 结论不变：本规范**必须同时覆盖全库级条款与表单专项**，表单专项不宜单独成文。

### 评估基准（重要，2026-09-24 用户明确）

> **仓库内现有组件（`button.tsx` / `input.tsx` 及其 story、test）均为临时演示与验证用的临时实现，不构成规范依据。**
>
> 定规范时**不得**以「与现有实现一致」「改动面最小」作为论据权重；只按**客观设计合理性**定条款，实现反向对齐规范。
>
> 影响：§3-C 的推荐曾基于「改动面最小」支持并入 `size` 轴，该论据在此基准下失效，结论已修正为正交维度（C-1）。此前所有以「实现现状」为权重的判断均需按此基准复核。

---

## 条款分级约定

| 标记          | 含义                                   | 违反后果                     |
| ------------- | -------------------------------------- | ---------------------------- |
| **MUST**      | 强制，所有组件契约与实现必须遵守       | 契约不予通过                 |
| **SHOULD**    | 推荐，偏离需在契约「设计决策」写明理由 | 评审时需说明                 |
| **MAY**       | 可选，按组件类型自定                   | 无                           |
| **EXCEPTION** | 已登记的例外，须在 §11 登记表有记录    | 无登记的例外按违反 MUST 处理 |

---

## 骨架（章节与条款占位）

### 0. 文档信息

版本 / 状态 / 负责人 / 最后更新 / 关联文档（Token 映射表、MVP 清单、各组件契约）。

### 1. 适用范围与强制性

- 适用对象：本库全部组件的契约与实现。
- 条款分级：见上方「条款分级约定」。
- **冲突处理规则**：本规范与单个组件契约冲突时，**以本规范为准**；确需例外者走 §11 例外登记。
- 与《CSS Token 映射表 V1》的分工：映射表管 **Token 的值与命名**，本规范管 **Token 的使用方式**。

### 2. 技术底座与运行时约定

- 无头库：**Base UI**（`@base-ui/react`），已替代 `radix-ui`。
- `asChild` **已废弃**，统一用 `render` prop（`Button` 即用 `useRender`）。
- React 19：`ref` 作为普通 prop，**不使用 `forwardRef`**。
- Tailwind v4（CSS-first，无 `tailwind.config`）。
- **无头库能力优先（MUST）**：Base UI 已内建的能力**不得自行实现**——受控双模式（`value` / `defaultValue` / `onValueChange`）、id 生成（`useLabelableId`）、label 关联（`Field.Label`）、`aria-describedby` 串联（`Field.Error` / `Field.Description`）、校验（`Field.Root` 的 `validate` / ValidityState）。
- **命令式能力约定（MUST）**：`ref` 一律指向 DOM 元素；确需命令式方法时仿 Base UI 用独立 `actionsRef`，**不占用 `ref`**。

> **待裁决**：无。

### 3. 尺寸体系

**已可固化（两份契约已一致，定为 MUST）**：

- **MUST** `size` 枚举统一为 `small` / `medium` / `large`，默认 `medium`。
- **MUST** 高度阶梯 **24px（`h-6`）/ 32px（`h-8`）/ 40px（`h-10`）**，三档。
- **MUST** 字号映射 **12px（`text-xs`）/ 14px（`text-sm`）/ 14px（`text-sm`）**。
- **MUST** 图标尺寸映射 **14px（`size-3.5`）/ 16px（`size-4`）/ 16px（`size-4`）**。
- **MUST** 实现向契约对齐（`button.tsx` 仍为 shadcn 官方 `default`/`xs`/`sm`/`lg`/`icon*` 集，需改造）。

**已裁决**：

| #     | 议题                                             | 裁决                                                                                                                                                                                                                       | 日期       |
| ----- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| **A** | 控件圆角：`radius/sm` **4px** vs `radius/md` 6px | **统一 `radius/sm`（`rounded-sm`，4px）**（方案 A2）。判定依据：`--radius: 0.625rem` 阶梯下 `--radius-sm = calc(--radius/5*2) = 4px`（`src/global.css:148`），**无需改 Token**，只改实现与契约。                           | 2026-09-24 |
| **B** | 「带图标时内边距收窄」是否为全库规则             | **定为全库 SHOULD，只写原则、档位归组件契约**（方案 B2）。条款：控件含图标时 **SHOULD** 收窄水平内边距以补偿图标的视觉留白；具体档位由各组件契约按自身布局定义；偏离须在契约「设计决策」写明理由。                         | 2026-09-24 |
| **C** | icon-only 尺寸是否纳入规范                       | **纳入为与 `size` 正交的独立维度**（方案 C-1）。`size` 固定 `small`/`medium`/`large` 三档；icon-only 由独立 prop（命名候选 `iconOnly` / `shape="square"`）派生正方形，边长 = 同档高度；icon-only **MUST** 提供无障碍名称。 | 2026-09-24 |

**待裁决**：无——§3 三项已全部裁决。

#### B2 条款落位

写入 §3 正文（新增 MUST/SHOULD 混排小节）：

> **SHOULD** 控件在含图标时收窄水平内边距，以补偿图标自带的视觉留白。
> **MAY** 具体收窄档位由各组件契约按自身布局机制定义（flex 子项用 `has-[>svg]:px-*`；affix 绝对定位布局另议）。
> **EXCEPTION** 偏离须在组件契约「设计决策」写明理由。

> 依据：动因（图标视觉补偿）具普遍性 → 值得进规范；实现机制随布局变化 → 不宜用 MUST 强加；本项目已有 SHOULD 分级语义（偏离需写明理由），两层各归其位。

#### C-1 条款落位

写入 §3 正文：

> **MUST** `size` 枚举固定为 `small` / `medium` / `large` 三档，**不得**为 icon-only 增设尺寸档位。
> **MUST** icon-only 控件为正方形，边长 = 同档 `size` 的高度（24 / 32 / 40px）；通过独立 prop 表达（命名候选 `iconOnly` / `shape="square"`），**不得**并入 `size` 枚举。
> **MUST** icon-only 控件提供无障碍名称（`aria-label` 或等价机制）。
> **SHOULD** 需要 `IconButton` 语义时，以薄封装复用 Button 的 variant 体系，不复制 variant 定义。

> 依据（客观设计合理性）：`size`（尺度）与 icon-only（内容形态）为正交维度。并入同轴会导致 ①枚举乘性增长（n 尺度 → n icon 档，新增尺度须同步新增 icon 档）；②组合语义未定义（`size="icon-sm"` + 文本子节点无定义）；③命名不对称（默认档 `icon` vs `icon-sm`/`icon-lg`）。正交后：尺度轴闭合、新增尺度自动获得对应 icon 档、无歧义组合。

> 待定子项：prop 命名（`iconOnly` vs `shape="square"`）留待 Button 契约起草时定，规范只约束「独立维度」这一原则。

#### A2 落地清单（已核实，待批量执行）

| 文件                                            | 改动                                                                                                                       | 性质     |
| ----------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | -------- |
| `src/shadcn-ui-lib/ui/button.tsx`               | `rounded-md` → `rounded-sm`，共 **7 处**（基础类 L8 + size 变体 L23/24/25/27/28/29）                                       | 实现     |
| `src/shadcn-ui-lib/ui/input.tsx`                | 类串 `rounded-md` → `rounded-sm`，**1 处**（L10）                                                                          | 实现     |
| `src/shadcn-ui-lib/ui/__tests__/input.test.tsx` | L64 断言 `toContain('rounded-md')` → `'rounded-sm'`                                                                        | 测试     |
| `registry/button.json` / `registry/input.json`  | 各含 7 / 1 处内嵌 `rounded-md`，**跑 `node scripts/generate-registry.cjs` 重生成**，禁止手改                               | 产物     |
| Button 契约（飞书 rev 1497）                    | 已为 `rounded-sm` 4px，**无需改**                                                                                          | 契约     |
| Input 契约（飞书 rev 226）                      | `--radius-md` / `rounded-md` → `radius/sm`，**需飞书回写**                                                                 | 契约     |
| `.agents/skills/figma-ai-bridge/SKILL.md`       | L74 圆角指引「圆角用 `rounded-md/lg`」→ 控件 `rounded-sm` / 表面 `rounded-md`·`lg`（**已改**，同批还修了 5 处 Radix 残留） | 开发指引 |

> 冗余提示（P2，非 A2 范围）：`button.tsx` 基础类已有圆角，6 个 size 变体又各写一次，属重复声明；建议后续单独清理，不与本次值替换混在一个 diff。

**其他需在 §3 明确的规则**：

- 内边距的 Figma 变量名（Button 契约已给 `spacing/md` / `spacing/lg` / `spacing/xl` / `spacing/2xl`；高度与字号在 Button 契约中 Figma 列均为 `—`，属合规的「无非标值」标注）。
- 高度/字号/图标三者的**联动约束**（选 `size` 即同时决定三者，不允许单独覆盖）。
- **例外登记方式**：`underlined` 类「圆角 0」如何登记（非标值须在 figma 列显式标注）。
- 触控目标下限（WCAG 2.2 2.5.8 最小 24px）——`small` 档 24px 恰在下限，移动端是否允许使用需明确。

### 4. 状态语言

**4.0 状态全集**

对齐 MVP 清单 §7 DoD：默认 / hover / focus / active / disabled / loading / error / empty / readonly。

**4.1 各态统一表达手段**（**草案，未逐项裁决**——下列为建议稿）

| 态       | 建议的统一手段                                 | 建议分级                 |
| -------- | ---------------------------------------------- | ------------------------ |
| default  | 基准                                           | —                        |
| hover    | 换底色 / 换描边（**禁止**整体 `opacity`）      | 见 4.2                   |
| focus    | 焦点环（口径见 §5）                            | MUST                     |
| active   | 底色再加深一档                                 | SHOULD                   |
| disabled | token 化禁用色（见 4.3）                       | MUST                     |
| loading  | 保留尺寸占位 + 禁止交互 + 进度指示             | SHOULD                   |
| error    | 描边换 `destructive` + 错误环 + 辅助文案（§5） | MUST                     |
| empty    | 内容区占位文案（非控件态）                     | MAY                      |
| readonly | 弱底色 + 保留描边与焦点环（见 4.4）            | MUST（仅适用可录入控件） |

**4.2 hover 的必选 / 可选归属（方案 HV-1 已裁决）**

> **MUST** 可点击 / 可操作类控件（Button 及同类）必须有 hover 反馈。
> **SHOULD** 文本录入类控件（Input / Textarea 及同类）提供轻微 hover 反馈（描边或底色变化）。
> **MAY** 录入类在契约「设计决策」写明理由后豁免 hover。
> **MUST** hover 反馈不得使用整体 `opacity`。

> 依据：hover 的语义是「该元素可被操作」的预反馈，对可点击类属必需；录入类的关键态是 focus，hover 属可选增益。

**4.3 disabled 口径（方案 DS-1 已裁决）**

> **MUST** 禁用态由语义 token 决定：底色 `bg-disabled`、描边 `border-disabled`、文字用弱化前景；**禁止**整体 `opacity` 降透明度方案。
> **MUST** 按 variant 分类给细则：填充类（default / destructive / secondary / outline）换禁用底色与描边；纯文字类（ghost / link）仅弱化文字。
> **MUST** 禁用态**不要求**对比度达标——WCAG 1.4.3 / 1.4.11 对 inactive 组件有豁免；但豁免须在组件契约中**显式声明依据**。

> 依据：token 化保证确定性、可审查、可随主题切换；整体 `opacity` 非 token 化，观感随底层背景浮动，且描边与内容一并淡化发灰。仓库已有 `--disabled`（`oklch(0.885 0 0)`，`src/global.css:65`，已映射 `--color-disabled`）即设计意图证据。

**4.4 readonly 口径（方案 RO-1 已裁决）**

> **MUST** readonly 与 default 视觉可区分。
> **MUST** readonly 与 disabled 可区分：readonly 仍可聚焦、可选中复制、会被提交，故 **MUST** 保留焦点指示，**MUST NOT** 使用禁用态配色。
> **SHOULD** 表达方式：底色 `bg-muted`、描边保持、文字保持 `foreground`（内容有效，不弱化）、焦点环保留。

> 依据：readonly 的语义是「内容有效但不可编辑」，视觉须传达「可读、可复制、但改不了」，且不得与 disabled 混淆。

### 5. 聚焦与错误指示

**5.0 实测基线**（亮色主题，环邻接页面背景 `#F2F4F8`；用 sRGB→OKLab 脚本实测，非手抄）

| 环色                      | 100%      | 50%       | 达 3:1 所需最小 α |
| ------------------------- | --------- | --------- | ----------------- |
| `--ring` (#3091E1)        | 3.05:1 ✅ | 1.71:1 ❌ | **98.7%**         |
| `--destructive` (#FD2237) | 3.49:1 ✅ | 2.09:1 ❌ | **78.9%**         |

> 关键推论：`--ring` 对浅背景的对比度上限仅 3.05:1，故**任何肉眼可辨的透明档都无法达标**；`/50` 是结构性不达标，不是可调项。

**5.1 聚焦环（方案 FR-3 已裁决）**

> **MUST** 聚焦环统一 **3px** 宽 + **50% 不透明档**（`ring-[3px] ring-ring/50`）。
> **MUST** 该档位不满足 WCAG 1.4.11 的 3:1；**合规性须由「描边」承担**（见 5.3 / 5.4）。

**5.2 焦点伪类归属（方案 FV-1 已裁决）**

> **MUST** 录入类控件（Input / Textarea 及同类）使用 `:focus`——鼠标点入也必须显示焦点指示。
> **MUST** 可点击类控件（Button 及同类）使用 `:focus-visible`。

> 依据：录入类的焦点位置是**必要信息**（用户须知道正在哪个字段输入）；可点击类用 `:focus-visible` 可避免鼠标点击时的视觉噪音。

**5.3 错误指示（用户裁决：3px / 50%）**

> **MUST** 错误态描边换 `--destructive`（100%，3.49:1 ✅），由描边承担 1.4.11。
> **MUST** 错误环统一 **3px** 宽 + **50% 不透明档**（`ring-destructive/50`，2.09:1）。
> **MUST** 组件契约须显式声明「**描边为错误态的主要指示，环为强调装饰**」，据此满足 1.4.11。

**5.4 对比度门槛与「主要指示」原则**（已确认——随 §5.5 裁决一并成立）

> **MUST** 非文本对比度 ≥ 3:1（WCAG 1.4.11）由**主要状态指示**（描边 / 底色）承担。
> **MAY** 装饰性强调（如 50% 档环）不适用该门槛，但**必须**在契约中显式声明其为装饰、并指明主要指示是什么。

**5.5 无描边控件的焦点指示（用户裁决：保留原有设计 + 提示风险）**

> **裁决**：**暂不修改设计**。全库统一 3px / 50% 环；**不**引入环与控件的间隙（`ring-offset` / `outline-offset`），**不**为无描边控件补描边或提高档位。

**已知风险（必须随规范一并发布，不得隐去）**：

1. Button 的 `default` / `secondary` / `ghost` / `link` 变体在焦点态**只有一个可见指示**——3px / 50% 环，实测对比度 **1.71:1**，**不满足 WCAG 2.1 SC 1.4.11 的 3:1**。
2. 有描边控件（Input / Textarea / Button `outline`）**不受影响**：其 1px 描边在焦点态变为 100% `--ring`（实测 **3.05:1 ✅**），环为装饰强调，满足 5.4 的「主要指示达标」原则。
3. 受影响场景：键盘用户在上述无描边变体上定位焦点时指示偏弱，在低对比度显示器或强环境光下尤为明显。
4. 错误态同理：`aria-invalid:border-destructive` 在无描边变体上同样是死代码（无边框宽度可上色），错误指示亦仅剩环（实测 2.09:1 ❌）。
5. **后续处理路径（三选一，均未启动）**：① 引入环与控件的间隙（`ring-offset-*` / `outline-offset`），使环只需与页面背景对比而可取 100%；② 无描边控件的环取 100%（实测 3.05:1 ✅）；③ 在 §11 例外登记表正式登记该偏差。**当前状态属「已识别的已知偏差，暂缓处理」，尚未进入 §11 登记。**

**实测证据（真实 Chromium，键盘 Tab 触发 `:focus-visible`，等 `transition-all` 结束取值）**：

| 元素                       | 聚焦后 border 宽度 | 聚焦后 border 颜色        | box-shadow 环层    | 可见的焦点指示                |
| -------------------------- | ------------------ | ------------------------- | ------------------ | ----------------------------- |
| Button `default`（无描边） | **0px**            | 已变 ring 色但宽 0 不可见 | `…/0.5) 0 0 0 3px` | **仅 3px 50% 环 = 1.71:1 ❌** |
| Button `outline`（有描边） | 1px                | ring 色 ✅                | `…/0.5) 0 0 0 3px` | 1px 描边 **3.05:1 ✅** + 环   |
| Input（有描边）            | 1px                | ring 色 ✅                | `…/0.5) 0 0 0 3px` | 1px 描边 **3.05:1 ✅** + 环   |

> 机制依据：Tailwind v4 preflight（`node_modules/tailwindcss/preflight.css:15`）对 `*` 设 `border: 0 solid` → 全局 border-width = 0；`button.tsx:8` 基础类只有 `focus-visible:border-ring`（**只设颜色、不设宽度**），`border` 宽度类仅出现在 `outline` 变体（`button.tsx:16`）。故无描边变体上该声明不产生可见效果——**这不影响环的渲染**（环走 box-shadow，与 border 无关）。
>
> 叠加事实：`--ring` 与 `--primary` **同值**（`#3091E1`，`src/global.css:21` vs `:40`，两个独立 token），故在 `default`（primary 填充）按钮上 100% 环与按钮底色同色；`/50` 因更浅反而可辨，但代价即上述 1.71:1。

- 与映射表「落地规则」小节的一致性：映射表为 `focus:ring-2` + 无透明档，本次裁决为 `ring-[3px]` + `/50` → **属双向偏离**，需反向修订映射表（该小节**只在线上**，离线快照无）。

### 6. 可访问性基线

- id 生成归属（**统一走 Base UI `Field`**，组件自身不生成）。
- `aria-describedby` / `aria-invalid` / `aria-required` 的归属与串联方式。
- 键盘操作基线（Tab / Enter / Esc / 方向键的通用要求）。
- 对比度门槛（正文 1.4.3 **≥ 4.5:1**；非文本 1.4.11 **≥ 3:1**）与**豁免声明规则**（豁免必须在契约中显式写明并说明依据）。
- `prefers-reduced-motion` 的降级要求（复用既有 `usePrefersReducedMotion`）。

### 7. 动效

- 时长 / 缓动（MVP 清单 §3 已定 150 / 200 / 300ms、ease-out）。
- 属性范围与减少动态效果的降级方式。

### 8. 表单组件专项

| 小节 | 内容                                                                                             | 状态                                    |
| ---- | ------------------------------------------------------------------------------------------------ | --------------------------------------- |
| 8.1  | **`Field` 集成**：id / label / error / description 的归属与串联                                  | 待起草（依赖 D7 的 Field 契约同期起草） |
| 8.2  | **辅助文本体系**：label / help / error / 字符计数的组合与间距                                    | 待起草                                  |
| 8.3  | **`required` 的视觉与语义**（含 Input 契约 §9 承诺 `aria-required` 但无 `required` prop 的缺口） | 待起草                                  |
| 8.4  | **校验与错误文案规范**（长度上限 / 是否带图标 / `role="alert"` / `aria-live`）                   | 待起草                                  |
| 8.5  | **受控 / 非受控口径**（对齐 Base UI `value` / `defaultValue` / `onValueChange`）                 | 待起草                                  |
| 8.6  | **IME（中文输入法）**：组合态处理与 `maxLength` 计数口径                                         | 待起草                                  |
| 8.7  | 表单组件的尺寸与状态（引用 §3 / §4，不重复定义）                                                 | 引用                                    |

### 9. 分发约定

- registry item 命名、落盘路径 `<aliases.ui>/shadcn-ui-lib/<name>.tsx`。
- `data-slot` 命名规则（跨组件选择器依赖它，如 InputGroup 的 `has-[input[aria-invalid=true]]`）。
- `registryDependencies` **必须写绝对 URL**（裸名会被解析为官方 `@shadcn` 同名组件）。
- 依赖声明规则（如基于 Base UI 的 item 需 `deps: @base-ui/react`）。

### 10. 内容与文案

- `placeholder` 长度上限与对比度归属。
- 错误文案长度上限与结构。

### 11. 例外登记表

| 组件       | 条款 | 例外内容 | 理由 | 决策人 | 日期 |
| ---------- | ---- | -------- | ---- | ------ | ---- |
| （待登记） |      |          |      |        |      |

### 12. 变更记录

| 版本 | 日期 | 变更说明 |
| ---- | ---- | -------- |
| v0.1 | 待定 | 首次创建 |

---

## 附录：本规范需收口的既有问题清单

来源：`input-contract-review-rev226.md`。下表为规范条款与该评审问题的映射，用于确认「无遗漏」：

| 评审编号 | 问题                                             | 归属条款                         |
| -------- | ------------------------------------------------ | -------------------------------- |
| F20      | 契约未声明技术底座                               | §2                               |
| F2       | `size` 枚举两处失效（`default` / `lg` 残留）     | §3                               |
| F6       | 聚焦环 / 错误环口径不达标且偏离映射表            | §5                               |
| F7       | 禁用态轮廓消失                                   | §4                               |
| F8       | 变体边界 3:1 未裁决                              | §4 / §5                          |
| F5       | placeholder 达标论证不成立                       | §6 / §10                         |
| F4       | §9 引用已移除的 `suffix`                         | §6 / §8.4                        |
| F5       | `aria-required` 无对应 prop                      | §8.3                             |
| F3       | Ref 语义矛盾                                     | §2                               |
| F11      | 组合规则覆盖不足                                 | 各组件契约（非规范）             |
| F12      | 表列命名混用                                     | 各组件契约（非规范）             |
| F14      | 尺寸表缺 Figma 列                                | 各组件契约（非规范）             |
| F15      | 暗色结论与仓库现状冲突                           | §4 / §6                          |
| F16      | 响应式与实现不一致                               | §3 / §4                          |
| F17      | 无「继承的原生 Props」表                         | 各组件契约（非规范）             |
| T4       | IME 未提                                         | §8.6                             |
| T5 / T6  | 字段样式常量（`fieldRings` / `fieldSizeStyles`） | §3 / §5 落地为共享常量           |
| 铁律 11  | 缺「可拆分复用工具」清单                         | 各组件契约；落点见 MVP 清单 §4.7 |
