# Input 组件 API 契约 — 设计侧评审（设计错误 + 设计缺口）

- 评审对象：[Input 组件 API 契约](https://kcnq8ppkkxh2.feishu.cn/wiki/Ws8nw41MWiNXVNkk7gRc2UgJnBg)（rev 101，状态「草案」）
- 上游依据：《CSS Token 映射表》rev 893（`HPgCdifqwoKaYSxPpSKc2Qy3nGr`）
- 横向基准：《Button 组件 API 契约》（rev 1047，状态「评审中」）
- 范围：**仅设计侧**。不涉及本地组件实现，实现侧问题见 `input-contract-gap-analysis.md`
- 评审日期：2026-09-22

---

## 1. 结论

契约最大的设计问题不是"缺字段"，而是**三个视觉变体在默认页面背景上都不构成可辨识的输入区域边界**，而契约第 9 节又声称"需满足 WCAG AA"——两者直接冲突。

| 分级     | 数量 | 说明                                      |
| -------- | ---- | ----------------------------------------- |
| 设计错误 | 9    | 可判定对错，含 6 条量化对比度不达标       |
| 设计未决 | 6    | 文档自己标了「待确认 / 待补充」，尚未裁决 |
| 设计缺失 | 12   | 设计规范应当定义但完全空白                |

---

## 2. 设计错误

### E1（严重）三个视觉变体均无法界定输入区域

按 Token 映射表取值，在页面底 `#F2F4F8` 上实测：

| 变体      | 界定方式                     | 与页面底对比度 | WCAG 2.1 1.4.11 非文本对比度（3:1） |
| --------- | ---------------------------- | -------------- | ----------------------------------- |
| `outline` | `--input` #D9D9D9 描边       | **1.28:1**     | 不达标                              |
| `filled`  | `--muted` #F0F0F0 底、无描边 | **1.03:1**     | 不达标                              |
| `ghost`   | 无描边、无底                 | **1.00:1**     | 不达标                              |

根因：明度阶梯 `#F0F0F0` < `#F2F4F8` < `#F5F5F5` 三级之间仅差约 0.01–0.015 明度，而描边 `#D9D9D9` 与页面底相差过大到"看不见"的程度——实际上三者都落在同一个明度带里。

**两个可选解法（需设计裁决）**：

1. 改 token：描边需深于 **#8D8D8D**（实测 3.01:1 临界）才能达标；`#999999` 也只有 2.59:1，仍不够。
2. 不改 token：在设计中明确声明"输入框边界依赖 `label` + `placeholder` + 值文本识别，边界描边豁免 1.4.11"，并把视觉重量转移到 `focus` / `hover` / 已填写态。

无论选哪个，契约 2 节的变体表与 7 节的状态矩阵都必须给出明确结论，不能只写"带 `--input` 描边"。

### E2 `filled` 变体与 `hover` 态色值撞车

Token 映射表中 `--muted` #F0F0F0（filled 底）与 `--accent` #F0F0F0（用途标注为 "hover/强调底"）**完全同值**。因此：

- `filled` 变体用 `--accent` 表达 hover → 视觉无变化（1.00:1）
- 契约 7 节 hover 写"描边略加深"，但 `filled` / `ghost` 无描边 → 该描述对这两个变体无效

需为 `filled` / `ghost` 单独定义 hover 表达（提高底色深度 / 换用 `--secondary`，或改用统一的不透明度策略）。

### E3 `placeholder` 色值两选一，其中一个不达标

契约 9 节："placeholder 使用 `--muted-foreground` 或 `--subtle-foreground`，需满足 WCAG AA"。

Token 映射表把 `--subtle-foreground` #999999 的用途明确写为"三级文字/placeholder"，实测：

| 组合                    | 对比度     | AA 正文 4.5:1 |
| ----------------------- | ---------- | ------------- |
| #999999 on 页面 #F2F4F8 | **2.59:1** | 不达标        |
| #999999 on 白底         | 2.85:1     | 不达标        |
| #666666 on 页面 #F2F4F8 | **5.21:1** | 达标          |

"二选一"的写法等于把不合规选项留给了实现方。**设计必须收敛为单一值**——建议 `--muted-foreground` #666666；若设计坚持 #999999，则需在契约中写明"placeholder 视为非必要提示文本，豁免 1.4.3"。

### E4 禁用态底与描边同色，轮廓消失

`--disabled` #D9D9D9 与 `--input` #D9D9D9 同值（Token 映射表确认）。契约 7 节要求禁用态用 `--disabled` 底 + `--muted-foreground` 文字，结果是底与描边同色，输入框退化为一个无边色块。

附带：禁用文字 #666666 于 #D9D9D9 底上为 4.07:1，低于 4.5:1（WCAG 1.4.3 对 inactive 组件有豁免，但契约未声明豁免）。

### E5 聚焦环不透明度未定义，50% 档不达标

契约 7 节只写"`--ring` 聚焦环（`ring-2`）"，未定义透明档。`--ring` #3091E1 在页面底 #F2F4F8 上：

| 环的不透明度 | 合成色  | 与页面底对比度    |
| ------------ | ------- | ----------------- |
| 100%         | #3091E1 | **3.05:1** 达标   |
| 75%          | #60AAE7 | 2.27:1 不达标     |
| 50%          | #91C2EC | **1.71:1** 不达标 |

若沿用项目常见的 `ring-ring/50`，聚焦指示将低于 1.4.11 的 3:1 要求。**设计必须写明环的不透明度**（建议 100%）与宽度。

> 更正上一轮判断：`ring-2`（2px）与 Token 映射表「落地规则」中的示例 `focus:ring-2` 一致；是 Button 契约/实现的 `ring-[3px]` 偏离了规范。设计侧 Input 无误，需统一的是 Button。

### E6 错误态红环透明档同样未定义

`--destructive` #FD2237 100% 时为 3.49:1（达标），但 20% 档仅 1.34:1、50% 档 2.09:1。契约 7 节写"常驻红环"却未给宽度与不透明度，实现无法落地。

### E7 `readOnly` 视觉与 `default` 完全一致

契约 7 节：readonly = "同 default，移除指针交互"。交互上"可聚焦不可编辑"，但视觉零差异，用户只能靠尝试输入失败来发现——可用性缺陷。设计上至少应给一项区分（底 `--muted` / 光标形态 / hover 无反馈 / 无聚焦环）。

### E8 附录 A 字号备注为事实错误

原文："Token 映射表字号基准为 `0.625rem`，与常见 14–16px 正文存在偏差"。

- `0.625rem` 是 `--radius` 圆角基准，与字号无关
- Token 映射表「八、字体行高」实为：`title-lg 18px` / `title-md 16px` / `body-strong 16px` / `body 14px` / `caption 12px`，行高统一 1.5

不存在"偏差"，尺寸—字号映射可直接落地。此条错误会误导设计确认环节。

### E9 变体枚举三处不一致 + 与圆角约定冲突

| 位置               | 声明                  |
| ------------------ | --------------------- |
| 第 2 节变体表      | 4 个，含 `underlined` |
| 第 3 节 Props 枚举 | 3 个                  |
| 总结               | 「三类视觉变体」      |

且 `underlined` 与附录 A「圆角统一 `rounded-md` 6px」冲突（下划线式通常圆角 0 或仅上方圆角）。另：第 2 节该行 Markdown 未加反引号，表格已渲染错列。

---

## 3. 设计未决（需出决策记录）

| #   | 位置     | 未决项                                 |
| --- | -------- | -------------------------------------- |
| O1  | 状态矩阵 | 9 行「设计稿」列全为「待补充」         |
| O2  | 元信息   | 设计稿链接、走查记录均为「待补」       |
| O3  | 第 8 节  | `error` + `disabled` 优先级未裁决      |
| O4  | 附录 A   | 字号与 `size` 的映射未定               |
| O5  | 第 7 节  | hover「描边略加深」无具体色值/工具类   |
| O6  | 第 7 节  | `error` 态常驻红环的宽度与不透明度未定 |

---

## 4. 设计缺失

### 4.1 必须有（缺则无法评审通过）

| #   | 缺失项             | 应含内容                                                                                                                                                 |
| --- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1  | **尺寸映射表**     | 高度 / 水平内边距 / 圆角 / 字号 / 行高 / 前后缀图标尺寸 / 最小宽度。Button 契约 2.5 有 5 列精确值，Input 仅「紧凑 / 默认 / 宽松」三个形容词              |
| M2  | **Figma 变量名列** | 全文无一处 Figma 变量名。Token 映射表已给出完整命名体系（`color/border/input`、`color/text/subtle`、`radius/md`、`spacing/lg`、`text/body`），可直接引用 |
| M3  | **变体映射表补全** | 现仅「说明」一列；应补 Token / Figma 变量名 / Tailwind 工具类 / CSS 值 / 视觉描述（Button 为 6 列）                                                      |
| M4  | **对比度实测表**   | 契约声称满足 AA，但全文无一处实测数值。应补「前景 / 背景 / 对比度 / 是否达标」四列表，本文第 2 节的数值可直接复用                                        |
| M5  | **变体选择指引**   | 何时用 `outline`、何时用 `filled`、何时用 `ghost`？表单默认 / 搜索框 / 表格内嵌的场景决策                                                                |

### 4.2 应该有（影响一致性与可用性）

| #   | 缺失项           | 应含内容                                                                                                                                                                                                            |
| --- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M6  | 前后缀视觉规范   | 与文本间距、图标随 `size` 的尺寸映射、是否带分割线、可点击前后缀的命中区域（≥ 24×24，WCAG 2.2 2.5.8）、禁用/错误态下的颜色继承                                                                                      |
| M7  | 辅助文本体系     | `label`（字号/颜色/与输入框间距/必填标记形态）、help text、error 文案（建议 `--text-caption` 12px + `--destructive`，图标与间距、换行与字数上限）、字符计数的位置与超限态                                           |
| M8  | 内置交互控件规范 | 清除按钮（契约 9 节提到 Esc 清空，但没设计这个按钮）、密码显隐切换、搜索图标                                                                                                                                        |
| M9  | 组合矩阵扩充     | 现仅 4 行，Button 有 14 行。缺：`error`+`focus`、`readOnly`+`disabled`、`size`+前后缀图标、`filled`+`focus`、`ghost`+`error`、密码框+显隐                                                                           |
| M10 | 动效规范         | transition 属性与时长、easing、错误态出现动效、聚焦环渐显、`prefers-reduced-motion` 降级（第 9 节只提了一句"降级"，无具体方案）                                                                                     |
| M11 | 响应式与触控尺寸 | 移动端触控目标高度（iOS HIG 44px / WCAG 2.2 最小 24px），`size="sm"` 是否会低于下限                                                                                                                                 |
| M12 | 暗色结论         | 附录 B「暂不实现暗色模式」是实现决策，不是设计结论。Token 映射表中 `--disabled`、`--subtle-foreground`、`--success` 等均标注「⚠️ 未重声明（继承亮色）」，暗色基线本身不完整。设计需给出"暗色规范是否定义"的明确结论 |

### 4.3 可选（国际化与内容）

- 输入内容溢出/超长文本的呈现（滚动还是截断、光标行为）
- RTL 下前后缀镜像规则
- 中英文占位文本长度差异（现仅 `placeholder ≤ 20 字`）
- 多语言错误文案的换行与截断

---

## 5. 建议补充的设计资产清单

1. **Figma 设计稿**：覆盖 3 变体 × 3 尺寸 × 8 状态（defult / hover / focus / disabled / error / readonly / empty / filled）+ 前后缀组合，并在状态矩阵「设计稿」列逐格回链（消掉 9 个「待补充」）。
2. **走查记录**：含对比度实测、键盘可达性、屏幕阅读器播报。
3. **对比度实测表**：本文第 2 节数值可直接作为初稿。
4. **尺寸规范图**：标注高度、内边距、圆角、字号、图标尺寸的标注图。
5. **辅助文本规范图**：label / help / error / 计数四者的组合版式与间距。

---

## 6. 建议的决策记录（DR）格式

针对 E1–E9 与 O1–O6，建议在文档顶部加一节「设计决策记录」，逐条记录：

| 议题                        | 结论 | 依据                                   | 决策人 | 日期 |
| --------------------------- | ---- | -------------------------------------- | ------ | ---- |
| 输入区域边界是否要求 3:1    | 待定 | 1.4.11 / 实测 1.28:1                   | —      | —    |
| placeholder 色值            | 待定 | #999999 = 2.59:1 ✗；#666666 = 5.21:1 ✓ | —      | —    |
| 聚焦环不透明度              | 待定 | 100% = 3.05 ✓；50% = 1.71 ✗            | —      | —    |
| `underlined` 是否保留       | 待定 | 与 `rounded-md` 冲突                   | —      | —    |
| `error` + `disabled` 优先级 | 待定 | —                                      | —      | —    |

---

## 7. 处理顺序建议

1. **先裁决 E1**：输入区域边界要不要满足 3:1。这条会连带决定 `--input` 是否改值、三个变体是否重设计，是其余所有视觉定义的前提。
2. **收敛色值口径**：placeholder（E3）、聚焦环（E5）、错误环（E6）、hover（E2）、禁用（E4）——输出对比度实测表。
3. **补尺寸体系**（M1）与 Figma 变量名（M2），并要求设计稿回链（O1/O2）。
4. **补辅助文本体系**（M7）与前后缀规范（M6）。
5. **修正文档错误**（E7–E9）并落决策记录。
